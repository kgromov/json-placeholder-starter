name: install-java
description: Install Java
runs:
  using: 'composite'
  steps:
    - uses: actions/setup-java@v4
      with:
        java-version: 25
        distribution: oracle
        cache: 'maven'